import 'package:flutter/material.dart';

class ElaTree extends StatelessWidget {
  const ElaTree({
    Key? key,
    required this.items,
  }) : super(key: key);

  final List<ElaTreeItem> items;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

class ElaTreeItem extends StatelessWidget {
  const ElaTreeItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
