import 'package:flutter/material.dart';

class InboundEditBatch extends StatelessWidget {
  const InboundEditBatch({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
